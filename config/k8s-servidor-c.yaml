apiVersion: apps/v1
kind: Deployment
metadata:
  name: servidor-c
  namespace: scalability-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: servidor-c
  template:
    metadata:
      labels:
        app: servidor-c
    spec:
      containers:
      - name: servidor-c
        image: guimsk/servidor-c:latest
        ports:
        - containerPort: 8000
        resources:
          requests:
            cpu: 100m
            memory: 681Mi
          limits:
            cpu: 200m
            memory: 1362Mi
      env:
        - name: SERVER_LANGUAGE
          value: "c"
